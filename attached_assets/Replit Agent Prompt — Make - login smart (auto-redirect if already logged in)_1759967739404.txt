You are my code editor/runner for BakerIQ (apps/app).

Goal: If a user is already authenticated and visits /login (e.g., via the Back button), automatically redirect them to the correct dashboard (legacy baker path by default: /baker/dashboard). Keep the current login flow unchanged for unauthenticated users.

# ================
# 0) Paths
# ================
- Client login page: apps/app/client/src/pages/Login.tsx
- Session probe (already exists): GET /api/session returns { authenticated, role?, ... }

# ================
# 1) Edit Login.tsx
# ================
Open apps/app/client/src/pages/Login.tsx and add a mount-time check that:
- calls /api/session with { credentials:"include", cache:"no-store" }
- if authenticated, redirects immediately to:
  - redirect param in URL (?redirect=/somewhere) if present, else
  - "/baker/dashboard" (legacy), else fallback "/dashboard"

Implementation details:
- Use window.location.search to read the ?redirect param (Wouter’s useLocation doesn’t provide search).
- Keep the existing form; just short-circuit when logged in.

Replace the component body with this exact version (preserve your styles and existing onSubmit logic):
--------------------------------------------------------------------------------
import { useEffect, useState } from "react";
import { useLocation } from "wouter";

export default function LoginPage() {
  const [, navigate] = useLocation();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [err, setErr] = useState("");
  const [checking, setChecking] = useState(true); // block UI until we know auth state

  // smart redirect if already logged in
  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const res = await fetch("/api/session", { credentials: "include", cache: "no-store" });
        const data = await res.json();
        if (!cancelled && data?.authenticated) {
          const params = new URLSearchParams(window.location.search);
          const desired = params.get("redirect");
          const target =
            desired && desired.startsWith("/") ? desired :
            "/baker/dashboard"; // legacy path; keep alias /dashboard available too
          navigate(target, { replace: true });
          return;
        }
      } catch {
        // ignore
      } finally {
        if (!cancelled) setChecking(false);
      }
    })();
    return () => { cancelled = true; };
  }, [navigate]);

  async function onSubmit(e: React.FormEvent) {
    e.preventDefault();
    setErr("");
    const res = await fetch("/api/login", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "include",
      body: JSON.stringify({ email, password }),
    });
    const data = await res.json().catch(() => ({}));
    if (res.ok && data?.ok) {
      navigate(data.redirect || "/baker/dashboard");
    } else {
      setErr(data?.error || "Login failed");
    }
  }

  if (checking) return <div style={{ padding: 24 }}>Checking session…</div>;

  return (
    <main className="min-h-screen flex items-center justify-center p-6">
      <div className="w-full max-w-sm space-y-4">
        <h1 className="text-2xl font-semibold text-center">Sign in</h1>
        {err && <div className="text-red-600 text-sm">{err}</div>}
        <form onSubmit={onSubmit} className="space-y-3">
          <input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" type="email" className="w-full border p-3 rounded" />
          <input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" type="password" className="w-full border p-3 rounded" />
          <button className="w-full p-3 rounded bg-black text-white">Sign in</button>
        </form>
      </div>
    </main>
  );
}
--------------------------------------------------------------------------------

# ================
# 2) Sanity tests
# ================
- With an active session, manually navigate to /login:
  EXPECT: immediate redirect to /baker/dashboard (or ?redirect target if provided).
- Without a session:
  EXPECT: login form renders normally and works as before.
- Hit Back from /baker/dashboard:
  EXPECT: if history returns to /login, it should immediately bounce you back to /baker/dashboard.

# ================
# 3) Report back
# ================
Return:
- The file edited (path)
- Confirmation that /login now auto-redirects when authenticated
- Any adjustments you made if the path/names differed
